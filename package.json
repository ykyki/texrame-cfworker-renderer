{
    "name": "texrame-cfworker-renderer",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev --remote --port 8787 --var URL_ORIGIN:'http://localhost:8787'",
        "test": "vitest"
    },
    "devDependencies": {
        "@cloudflare/puppeteer": "^0.0.6",
        "@cloudflare/vitest-pool-workers": "^0.1.0",
        "@cloudflare/workers-types": "^4.20240405.0",
        "mathjax": "^3.2.2",
        "typescript": "^5.0.4",
        "vitest": "1.3.0",
        "wrangler": "^3.0.0"
    }
}
